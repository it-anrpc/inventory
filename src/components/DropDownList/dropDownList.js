import React from "react";
import LayoutManager from "./layoutManager";

export default function DropDownList({ navigation, props }) {
  return <LayoutManager navigation={navigation} props={props} />;
}
